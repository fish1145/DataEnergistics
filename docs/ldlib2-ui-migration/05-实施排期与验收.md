# 实施排期与验收

## 工作包与依赖

| 编号 | 工作包 | 前置条件 | 产出 | 验收命令/方式 |
| --- | --- | --- | --- | --- |
| P0 | 依赖与开发入口 | 无 | LDLib2 依赖、插件、最小方块 UI | Gradle 编译；客户端打开/关闭 UI |
| P1 | 共用样式与绑定工具 | P0 | LSS、状态/tooltip/按钮组件 | 单元测试绑定转换；客户端截图核对 |
| P2 | Trinity 只读状态页 | P1 | 多方块状态面板 | 开发世界三种结构状态比对 |
| P2A | 双端窗口生命周期 | P2 | 静态 root coordinator、服务端权威 open/close/reopen、确定性注册拓扑 | 双树 sequence、ID 顺序、拒绝/乱序和 Esc 回归 |
| P3 | Trinity 交互页 | P2A | Pattern Core、四个业务 provider、退款与自动建造非模态浮窗 | 单人与多人交互回归 |
| P4 | 舱室基础和 ME 输出 | P1 | 背包和只读展示槽 | 槽位交互回归 |
| P5 | 复杂舱室 | P4 | 输入、组合仓库、模式缓冲 | 全槽位矩阵回归 |
| P6 | 预览模型/API 缺口 | P0 | 不可变结构快照；必要时独立 MDLib PR | common 层逻辑测试 |
| P7 | 通用 XEI 页面 | P1、P6 | JEI/EMI 类别与共用 UI | 两种浏览器交互回归 |
| P8 | AE Processing 配置 transfer | P6、P7 | 普通多方块配方通过 JEI/EMI typed handler 原子填入 AE Processing 配置 | input/output、真实槽位容量、回滚与既有编码按钮回归 |
| P9 | 清理和发布验证 | P3、P5、P7、P8 | 旧入口删除、文档更新 | 完整构建与手工验收 |

## 当前状态（实现锚点 `b5780774`）

发布批次以 `d546ae7c` 为基线，至实现锚点 `b5780774` 包含 8 个按功能拆分的提交，文档提交位于其后。P8 的 common/network、JEI 与 EMI 实现已经提交；最终本地自动化和质量审计已经完成，恢复时应读取实际 Git/PR 状态判断文档提交、推送和 Draft PR CI 是否仍需执行。

| 工作包 | 状态 | 证据与剩余门槛 |
| --- | --- | --- |
| P0-P2A | 已提交 | 依赖、AE/LDLib2 menu bridge、Data Core 首切片、四窗 host 和双端服务端权威 lifecycle 已完成。 |
| P3 | 已提交，客户端验收待补 | Pattern Core、`main/cpu/crafting/auto_build` 四个真实可拖动 provider、退款与自动搭建动作已接入；四窗统一 LDLib2 chrome、独立 session/Scene、可拖动标题栏、置顶 rail 和初始位置级联已经覆盖。旧 Data Core 和旧自动搭建生产入口已删除。 |
| P4-P5 | 已提交，客户端验收待补 | `ME_OUTPUT`、`ME_INPUT`、INPUT/OUTPUT 组合仓库和 `PATTERN_BUFFER` 已迁移 LDLib2，并完成舱室信息布局与本地化；P9 扫描确认保留的同名 Screen 只是 AE2 输入协议薄壳，不包含旧视觉树。 |
| P6 | 已提交 | MDLib projector/paired candidate、DataE atomic revision、preview selection/snapshot/material/recipe view 与 Trinity catalog 已完成。MDLib 验证为 GameTest 4/4。 |
| P7 | 已提交，客户端验收待补 | 共用 preview session/panel/factory、隔离 Scene 生命周期、JEI 与 EMI 页面已接入；多子结构 selector 使用横向 scroller，按钮最小宽度为 64px，两端共用 composition、候选栏、规范 input 和 owner output。服务端/逻辑验证不能替代客户端渲染验收。 |
| P8-A | 已提交 | common/network typed request、bounded codec/日志、服务端权威重建、真实目标容量预检、双库存原子回滚和 source/key/fluid/display 临时状态事务已完成。 |
| P8-B | 已提交 | JEI 对原生/Universal 菜单做精确 typed 注册，点击时读取当前 recipe view，预检 source/identity、slot filter、maxAmount 异常；延迟合并刷新动态 ingredient；runtime stop 时释放 category `uiCache`，下一次 start 可重新注册。 |
| P8-C | 已提交 | EMI handler/core/guard/mixin 仅在 `FILL_BUTTON` 接管 DataE typed live recipe，具体 handler 的 `supportsRecipe` 修复父类 catch-all；`getInputs()`/`getOutputs()` 暴露 live ingredients，槽池扩容后延迟 `focusRecipe` 刷新。 |
| P9 | 进行中 | 最终 `spotlessCheck compileJava compileTestJava test runGameTestServer build`、367/367 GameTest 和 diff/BOM/FQN/反射/`Objects.requireNonNull` 等质量审计已通过；隔离的客户端运行环境已能进入世界并完成 JEI/EMI reload。用户不要求新增客户端自动测试，仍需按 [独立手工验收表](09-客户端与多人手工验收.md) 完成真实客户端/多人操作，并按实际状态确认文档提交、推送和 Draft PR CI。 |

P9 的最终本地自动化 `./gradlew.bat spotlessCheck compileJava compileTestJava test runGameTestServer build --no-daemon` 已使用系统 `GRADLE_USER_HOME=E:/.gradle` 再次成功执行，服务端 GameTest 为 367/367。`clientTestRuntimeClasspath` 仅额外包含 Athena，生产 `runtimeClasspath` 和服务端 `testRuntimeClasspath` 均不包含它；可复现客户端 run 已加载 Athena 4.0.6、LDLib2 2.2.28、Oritech 1.2.8 并完成 JEI/EMI reload。该结果仍不等于客户端 UI 或多人验收成功；后续若重新构建，仍须先读取环境变量且禁止 `-g`。

P8 transfer 的职责到“填充 Processing 配置并切换模式”为止：不调用编码、不读写 `encodedPatternSlot`、不消耗空白样板、不搬运玩家或网络物料。实现会在同一事务中清空并验证旧 pending/last source、key、fluid 与 display fallback，保证普通多方块配方不继承 Data Ripper 或其他 XEI transfer 状态。只有用户随后点击 AE 自带编码按钮，才由既有 AE 流程生成普通 Processing Pattern。

## 发布提交边界

从发布基线 `d546ae7c` 至 UI 实现锚点 `b5780774` 的 8 个 UI 实现提交保持独立功能边界：前 5 个提交完成通用浮窗与 XEI 交互收口，后 3 个提交完成 Trinity、舱室和本地化收口。

| 提交 | 内容 |
| --- | --- |
| `36620546` | 完善 LDLib2 主机浮窗层级交互 |
| `e1027e0a` | 重构多方块预览交互布局 |
| `186bff09` | 重构通用 XEI 多方块预览布局 |
| `d14ee1ed` | 同步刷新 JEI 与 EMI 多方块原料 |
| `69ca9992` | 避免 LDLib2 子窗口初始重叠 |
| `956f2034` | 统一 Trinity 四窗 LDLib2 布局 |
| `cd7577ad` | 优化舱室 LDLib2 信息布局 |
| `b5780774` | 补充 LDLib2 UI 本地化文本 |

随后按验收边界继续拆分为独立提交：

| 提交 | 内容 |
| --- | --- |
| `24f35593` | 更新 LDLib2 UI 迁移验收文档 |
| `39b37fe6` | 以独立 `clientTest` source set 补充 Athena 客户端 GameTest 运行依赖，不进入生产或服务端 runtime |
| `1b332d0e` | 修复 EMI synthetic recipe id 与多方块 category 翻译，并保持 canonical typed transfer identity |

## 测试策略

| 层级 | 内容 |
| --- | --- |
| 纯逻辑测试 | 自动建造选择到 request 的转换、容量门控映射、分页边界、展示格式化。测试必须直接调用目标逻辑。 |
| 容器集成测试 | 方块 holder 有效性、菜单打开/关闭、服务端按钮授权、库存及假槽操作。 |
| 双端拓扑测试 | 分别构造 server/client 菜单树，断言 sync/RPC 注册顺序、初始数据 round-trip、动态 open/close/reopen sequence 与单调 ID 一致。 |
| 客户端手工测试 | 布局、tooltip、缩放、XEI 排除区域、鼠标键盘焦点、多人同步。 |
| XEI 一致性测试 | JEI 与 EMI 对同一选择生成相同快照、材料和样板请求；显示层切换不改变材料。 |
| transfer 事务测试 | transfer 后 input/output、Processing 模式、真实容量/过滤/数量、source/key/fluid/display 清理、前向故障、完整/不完整回滚和菜单失效；断言不消耗空白样板、不碰 encodedPatternSlot、不搬物料。 |
| 既有编码回归 | transfer 成功后由用户触发 AE 编码按钮，验证空白样板消耗、覆盖/目标槽竞争和普通 Processing Pattern 内容。 |
| 回归测试 | 旧 UI 与新 UI 在同一存档、同一宿主状态下显示和行为一致。 |

AE transfer 的边界值必须覆盖 81 个输入成功与 82 个输入原子失败；失败时 Processing 模式和全部原槽内容保持不变。JEI/EMI 在 tier/repeat 更新后的下一次 transfer 必须立即读取新 recipe view，不允许使用 ingredient 缓存中的旧材料。动态 ingredient 已在两端刷新，但 transfer 的唯一权威仍是点击瞬间的 `currentRecipeView()`。

P8-A 测试覆盖：未知 controller/structure/variant、越界 tier/repeat、未知 candidate、过期 revision、错误 registered recipe id、真实小容量、slot filter、amount 超限、前向第 N 次写入故障、切换 Processing 后 `endBatch` 故障、完整/不完整回滚、stale container 与同 id 错菜单。服务端只接收选择身份和 fingerprint，并从当前 catalog 重建材料；客户端不得提交可信材料栈。日志上下文只包含受限 identity 与 repeat/tier/candidate 数量，不展开客户端 map/value。

临时 source 状态测试必须先注入 pending/last workstation、Data Ripper key/fluid 与 display fallback，再验证成功 transfer 全部清空；随后在模式切换、状态清理、batch publication 等故障点验证这些字段与 mode/input/output 一起恢复。Data Ripper resolver 只允许明确 `DataRipperReassemblerRecipe` 或 `RecipeHolder`，不得从普通多方块 recipe、上下文提示或历史 source 推断。

客户端运行依赖已通过隔离的 `clientTestRuntimeOnly` 解决，且客户端能够进入世界、完成 JEI/EMI reload；DataE 自身的 EMI synthetic id 与 category 翻译告警也已在修复后消失。用户已确认不要求客户端自动测试或执行汇总，真实页面、按钮、槽位和多人行为由 [09-客户端与多人手工验收.md](09-客户端与多人手工验收.md) 逐项记录。不得将 367/367 服务端 GameTest、JEI/EMI 定向 JUnit、IDEA build 或插件初始化写成客户端渲染和交互已通过。

动态窗口测试覆盖：open request 经服务端 ack 后两端各创建一次；close 按钮和 Esc 在发 request 前不移除客户端树；拒绝、重复和乱序 sequence 不改变 membership；close/reopen 两端注册 ID 同步单调增长；四窗并行打开后保持独立 session/Scene，并按 z-order 关闭；菜单失效或断线时两端释放资源。默认位置使用真实 screen bounds 做碰撞检测并级联，保存位置和用户拖动位置不被覆盖；`320x240` 边界下四个 `292x210` 窗口依次落在 `(4,4)`、`(11,11)`、`(18,18)`、`(24,25)`。不得用预挂载隐藏窗口让测试绕过动态注册。

JEI 公共 API 没有对既有 formal slots 的原位 invalidation。当前动态选择先合并延迟刷新请求，随后通过公开 `showRecipes` 重建页面/formal slots，并接受导航历史增加这一可见副作用。禁止以反射或 JEI 私有实现规避该边界。

自动搭建恶意请求必须覆盖未知 `structureKey`/`variantIndex`、越界 tier/repeat、过期 definition revision、伪造或重放 sequence、距离/权限不足、区块未加载、库存检查后变化、目标方块冲突及菜单/host 已替换。每个失败断言同时验证世界零写入、库存零净消耗、无残留 build session，并有包含玩家、host 和请求参数的日志。

## 每个提交的门槛

1. 所有源码 UTF-8 无 BOM；新增 Java 代码仅使用 import，不写完全限定名。
2. 新增公共接口按项目规范提供类和成员动机注释；不使用反射处理 AE2 或 LDLib2 访问问题。
3. 新功能的服务端状态变更均有权限、宿主有效性和参数校验；异常记录有上下文日志。
4. 执行与变更匹配的 Gradle 测试，并在客户端验证 UI 非空、交互可用。
5. 当前 Data Energistics 与 MDLib 分支均已明确授权继续推送现有 Draft PR；新增仓库或扩大 Git 范围仍需重新确认。提交信息遵循仓库既有 Git 风格。
6. Gradle 只使用系统 `GRADLE_USER_HOME`；未设置或不位于 `E:/` 盘则不构建，不创建任何替代缓存目录。
7. 清理本次新增或直接修改代码中的冗余 `Objects.requireNonNull`，仅保留公开不变量、延迟 supplier 或确有必要的过滤校验。
8. 所有 LDLib2 行为以项目当前实际依赖的 `2.2.28` source jar 为准；ECO 的 `2.2.1` 只能作为旧版参考。含动态元素的提交必须证明 client/server 注册拓扑和 sequence 一致。
9. 对 `removeChild`、`REMOVED`、`MUI_CHANGED` 与资源释放做 fault-injection；异常后 host 必须 terminal，后续资源继续尝试释放，残留树不得复用或通过反射修补。
10. 合理使用 Lombok：只减少 getter/setter 等机械样板；表达不可变数据和结构化值的 `record` 保留，不为统一风格改成可变 POJO。

## 提交策略

1. 由主代理处理 Git；子代理不提交、推送或更新 PR。
2. P8 已拆为 common/network 原子 transfer、JEI typed adapter 与 EMI typed adapter；后续文档/验收继续单独提交，逐个检查 staged diff，禁止 `git add .`。
3. 每个提交只带对应实现和测试。最终 `test build` 与质量审计已经完成；逐文件提交文档后，按实际 tracking ref 决定是否推送 `qy/ldlib2-ui-migration`，更新 Draft PR #99 并等待 CI。
4. MDLib 仅在确认新的中立 API 缺口时使用隔离 worktree，以独立提交、测试、推送和 PR 处理；不得与 DataE 提交混合。

## 回滚条件

出现任一情况时保留旧 UI 注册并停止扩大迁移：物品重复/丢失、可绕过升级或容量限制、client/server 窗口树或 RPC ID 分歧、多人状态不同步、自动建造参数被客户端伪造后仍获执行、LDLib2 清理异常后继续复用残留树、XEI 交互被遮挡且无法修复。每个工作包独立提交，确保可按工作包回退。
