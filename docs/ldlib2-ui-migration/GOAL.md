# 可直接使用的 Goal 文本

以下文本是当前已经启动的持久 Codex Goal；上下文恢复时以本文件和 `00-需求基线与上下文恢复.md` 为准。

```text
在 F:/mc/Fish_Dan_/DataEnergistics 中完成多方块 UI 与全部舱室 UI 向 LDLib2 的迁移，并实现通用 XEI 多方块预览、子结构配置，以及普通多方块 XEI 配方到 AE Processing 样板的 transfer/编码。开始前完整阅读 docs/ldlib2-ui-migration 下全部文档，以 00-需求基线与上下文恢复.md 为不可变需求入口；上下文压缩后必须重新读取该目录，不得依赖记忆补全需求。

参考源码：F:/mc/ldlib/LDLib2、F:/1.21.1/NeoECOAEExtension、F:/mc/ExtremelyFrozen/fork/GregTech-Modern。MDLib 本任务隔离 worktree：F:/mc/Fish_Dan_/Modular-Data-lib-preview-api；原始 F:/mc/Fish_Dan_/Modular-Data-lib 含用户改动，只可只读参考。优先使用 IntelliJ IDEA MCP 读取、检索、检查和构建已打开工程，只有未在 IDEA 打开的隔离 worktree 才使用终端。先审计工作区与现有实现，保留所有非本任务改动。采用主代理负责需求、集成验证和 Git，子代理仅处理边界清晰的只读分析、实现、测试或审查；主代理必须审阅实际 diff 和验证结果。

当前状态必须作为续做起点：DataEnergistics 位于 qy/ldlib2-ui-migration，Draft PR https://github.com/fish1145/DataEnergistics/pull/99。除既有设计/依赖提交外，已按功能提交 2df0ed28（atomic definition snapshot）、5cdc4ae7（MDLib paired jar）、89a81c7b（DataE predicate pair 适配）、a74fe20a（common preview 模型）、68a2f3e8（Trinity preview catalog）、dd41ae4e（Lombok getter 与共享 key）、330f4c8b（AE/LDLib2 menu bridge 与 AeItemSlot）、d69b6cbe（四个多方块子 UI 身份）、1752be71（可拖动 HostUiExtension）、30d6d5d1（失败挂载完整清理）和 1aeb1cf8（Trinity Data Core 首个 LDLib2 垂直切片）。P2A 已继续拆为 8eb22095（生命周期协议模型）、7fc43b88（双端协调）、faecf369（生命周期网络）、e524687d（Trinity coordinator 挂载）。MDLib 位于 qy/multiblock-preview-api，Draft PR https://github.com/ModularMCLib/Modular-Data-lib/pull/2，已推送 cbbbbd7（PatternCandidate 与 patternCandidates），并通过 spotlessCheck/test/runGameTestServer/build，GameTest 4/4。DataEnergistics 的 P2A 已实现服务端权威 open/close/reopen、全 host 单 pending、fresh generation、root/后代 pending guard、terminal fault handling、exact menu/host payload routing，以及真实 SyncValue/RPC ID close/reopen 单调验证；IDEA 编译、spotlessCheck/test/runGameTestServer 已通过，GameTest 317/317。P2A 期间 Trinity 生产 host 的 provider 表暂时为空且两端同构；P3 必须在 coordinator attach 前由 TrinityDataCoreHostUi 按 main -> cpu -> crafting -> auto_build 注册四个真实 provider，禁止占位或 late registration。下一步从 P3 Pattern Core 与四个业务 provider 继续，不得重复 P2A，也不要再次等待现有 PR 的推送授权。

必须交付：
1. LDLib2 方块 UI：Trinity 多方块状态、故障、容量、Pattern Core 分页/退款、自动建造，以及 INPUT/OUTPUT、ME_INPUT、ME_OUTPUT、PATTERN_BUFFER 全部舱室界面。
2. 通用预览模型：controller、稳定 `structureKey`、结构内部 `variantIndex`、每个子结构独立 tier/repeat、candidate selections、definition revision，以及不可变的坐标/predicate/候选/材料/bounds/layer 快照。`visibleLayer` 只属于 `PreviewViewState`，必须与 variant/repeat 分离。
3. 共用 LDLib2 预览 UI：Scene 可旋转缩放和选中方块；支持子结构、variant、tier、repeat、全部/上一层/下一层/指定层、材料 scroller。JEI 使用 ModularUIRecipeCategory，EMI 使用 ModularUIEMIRecipe；host provider 与 XEI adapter 只委托同一个纯 UI factory 和快照模型，各自创建独立元素树、Scene 和 dummy world，为 REI 保持薄适配边界。
4. XEI ingredient：材料随 recipe selection 重算并正确暴露。只有 `MultiblockRecipeView.inputs()` 的规范汇总槽是 INPUT，controller/owner 规范槽是 OUTPUT；Scene、候选检查器、谓词详情、hover 与层级控件均为 NONE，辅助槽不得注册 recipe ingredient/slot。显示层和 formed 视觉开关不得改变材料。XEI `registeredRecipeId` 固定为 controller 级身份，动态选择使用包含 revision、structureKey、variantIndex、tier/repeat 与 candidate selections 的 `projectionFingerprint`。
5. AE 样板：借鉴 GT/ECO 清晰的普通 XEI 多方块页面模式，由 DataEnergistics 明确定义 recipe role 与 typed transfer，把当前展开结构转换为“聚合材料 input + 控制器/owner output”的普通配方。GT 没有现成多方块 AE writer，ECO upload 又拒绝 Processing，二者都不能作为完整编码实现。JEI/EMI 对 DataE 的普通配方执行现有 AE recipe transfer，填入 Processing 模式，再复用项目现有编码流程。不得新增 AE 专属配方、建造蓝图 item、custom pattern details 或直接写 encodedPatternSlot。超出终端容量时返回可见错误，不能截断或部分覆盖。
   写入结果必须是通用 Processing 样板：只含普通材料 input 与 controller/owner output。structureKey、variantIndex、tier/repeat 与 candidate selections 的选择结果只反映为普通 stack/amount；这些标识本身以及 visibleLayer、方向、镜像或精确 BlockState 均不持久化。具体结构和放置状态仅由独立 build request 在实际搭建时提交，并由服务端根据权威定义和当次参数自动补全。
6. 业务边界：复用现有多方块匹配、自动建造、舱室库存和 AE2 规则；UI 不复制权威业务逻辑，不使用反射，不写占位实现。
7. 主机 UI 扩展：提供可嵌入、可开关、可拖动的非模态子 UI provider/host 接口。Trinity 必须注册 `main`、`cpu`、`crafting` 与自动搭建共四个可同时打开的独立子 UI，自动搭建不是结构窗口页签；四窗分别拥有位置、z-order、元素树、Scene/资源和释放周期。以 WindowDragHelper 或 LDLib2 等价能力实现标题栏拖动、点击置顶、Esc 按最上层逐个关闭、viewport 约束和动态 XEI extra area。关闭时必须 removeChild 并释放资源，重开必须创建全新的 UI/Scene。主机与 XEI 只复用纯预览组件工厂，不共享 provider、UI 实例、Scene、相机、显示层或拖动状态。
8. 双端窗口协议：client/server 菜单必须以确定顺序构造相同的 sync/RPC 注册拓扑。始终挂载的 root coordinator 处理带 sequence 的 open/close request；服务端验证 key 与宿主后先变更权威动态树，再 ack 客户端应用同一操作。关闭按钮与 Esc 不得先本地移除所属树，双方 open/close/reopen 顺序必须一致且每次创建新树。禁止用 ECO 式预挂载隐藏窗口规避动态 ID；位置、z-order、相机、hover 和 visibleLayer 仅保留客户端。

通用结构投影 API 已在 MDLib Draft PR #2 中实现：包含 repeat layout/selection、controller 锚定的 layer/cell/source/bounds snapshot、非默认 StructureDir/flip、100 万 cell 与 matcher 访问预算、精确 BlockState/placement ItemStack 候选，以及 exact state + component-aware stack 的 `PatternCandidate` 配对。内置 predicate 直接给出 pair；legacy separate lists 只在关系可证明时转换，歧义 many-to-many Fail Fast。MDLib 不得继续扩入 LDLib2 UI、JEI/EMI、AE2、Trinity 子结构或材料选择业务。后续若审查要求修正，只在隔离 worktree 中按独立提交处理并推送现有 PR。

Gradle 只能使用系统环境变量 GRADLE_USER_HOME。每次构建前读取并记录该变量；未设置或解析后的路径不在 E:/ 盘则停止并报告。禁止创建或重定向项目内/用户目录 Gradle 缓存，禁止使用 -g 或修改构建脚本覆盖缓存位置。DataEnergistics 的 LDLib2 权威版本是 2.2.8；不得以 ECO 的 2.2.1 或 F:/mc/ldlib/LDLib2 当前 2.2.28 API 代替核对。源码与文档统一 UTF-8 无 BOM；Java 使用 import，不写完全限定名；清理本次新增或直接修改代码中的冗余 Objects.requireNonNull，只在延迟 supplier、公开构造不变量或确有必要的过滤边界保留。Lombok 仅用于确实减少 getter/setter 等样板代码的场景，不为使用 Lombok 改写清晰模型；适合不可变数据载体和结构化值的 record 必须保留。新增接口和成员按项目规范说明动机和作用；异常 Fail Fast 并记录上下文日志，核心业务避免未捕获异常导致进程崩溃。LDLib2 2.2.8 的 removeChild/onRemoved/MUI_CHANGED 异常清理并非强异常安全，HostSubUiRoot 只能降低资源泄漏风险；发布前必须覆盖 listener 抛错、同级资源继续释放、host terminal 和禁止复用残留树。DataEnergistics 后续每个工作包按功能独立提交，遵循仓库既有中文提交风格，不混入无关改动；由主代理统一审阅、验证和处理 Git 事务。

common preview spec/selection/snapshot/material/revision、Trinity catalog、AE menu bridge、AeItemSlot、支持四个独立窗口的 HostUiExtension、Trinity Data Core 首个 LDLib2 垂直切片，以及 P2A 双端 lifecycle coordinator 已完成并提交。从当前状态继续按以下顺序推进：P3 Pattern Core 与 main/cpu/crafting/auto-build 四个真实业务 provider；ME_OUTPUT、ME_INPUT、组合仓库、PATTERN_BUFFER；JEI/EMI 共用预览；typed AE transfer；旧入口清理。测试必须直接调用目标逻辑，禁止源码 contain 测试、删除功能测试和反射测试。完成每个工作包后更新文档状态，执行对应逻辑/容器测试；最终在 JEI、EMI、单人和多人环境验证 UI、材料、槽位、双端开关序列、自动建造、资源重载、恶意请求、样板原子性与渲染资源释放。自动搭建恶意请求必须覆盖未知 structureKey/variant、越界 tier/repeat、过期 revision、距离/权限/区块/库存变化、重复 sequence 和菜单/host 已失效；失败不得写世界或部分消耗材料。只有所有验收通过且无未处理阻塞项才可结束 Goal。
```
