# 需求基线与上下文恢复

## 当前任务状态

本文是上下文压缩后的权威恢复入口。用户已经通过持久 Goal 明确启动实施，并授权 Data Energistics 与确有 API 缺口的 Modular Data Lib 分别建立 `qy/` 分支、按功能提交、推送和创建 Draft PR。后续执行者不得退回“只写计划”或“等待再次授权”的旧状态。

当前 Git 事实：Data Energistics 使用 `qy/ldlib2-ui-migration`，Draft PR 为 [#99](https://github.com/fish1145/DataEnergistics/pull/99)；MDLib 实现位于隔离 worktree `F:/mc/Fish_Dan_/Modular-Data-lib-preview-api` 的 `qy/multiblock-preview-api`，Draft PR 为 [#2](https://github.com/ModularMCLib/Modular-Data-lib/pull/2)。原始 `F:/mc/Fish_Dan_/Modular-Data-lib` 含用户改动，禁止在其中修改、暂存或提交本任务内容。

## 不可变需求

| 编号 | 需求 |
| --- | --- |
| R1 | 将 Data Energistics 的多方块 UI 和所有舱室 UI 迁移到 LDLib2。 |
| R2 | 提供通用 XEI 多方块预览，至少由 JEI 与 EMI 共用同一个 LDLib2 UI 和预览模型；为 REI 保留薄适配边界。 |
| R3 | 预览支持在 Trinity 的 main、cpu、crafting 等子结构之间切换，不把子结构硬编码在 XEI 适配器中。 |
| R4 | 每个子结构可独立调整业务等级（tier）和重复层数/长度（repeat count）。 |
| R5 | 显示层切片与结构重复层数是两个不同状态；预览支持全部、上一层、下一层和指定层。 |
| R6 | 材料清单随子结构、等级和重复层数实时重算，并作为 XEI 输入 ingredient 暴露。 |
| R7 | 当前结构选择自动转换为普通 XEI 配方：聚合材料为 input，控制器/owner 物品为 output；通过现有 JEI/EMI recipe transfer 写入 AE Processing 样板。 |
| R8 | 方块 UI、XEI 预览和样板写入共享纯结构选择/快照模型，不共享客户端控件实例或虚拟世界实例。 |
| R9 | 如确实缺少通用多方块投影 API，未来可单独修改 Modular Data Lib 并提 PR；AE2 依赖和样板编码不得进入该基础库。 |
| R10 | 所有文件 UTF-8 无 BOM；Java 不使用完全限定名或反射；实现遵循 Fail Fast、现有接口规范和 Git 提交风格。每个工作包必须按功能独立提交，不把无关改动混入同一提交。 |
| R11 | 主机 UI 必须提供可嵌入、可开关、可拖动的非模态子 UI 接口；多方块预览/自动建造面板通过该接口挂载，不与 XEI 共享控件或拖动状态。关闭必须从元素树 `removeChild` 并释放资源，重开必须创建全新的 UI/`Scene`。 |
| R12 | 写入 AE 的必须是通用 Processing 样板，只持久化普通配方的材料 input 与 controller/owner output；子结构选择、tier/repeat、显示层、方向和具体 `BlockState` 属性不进入样板。具体方块状态仅在实际搭建时由结构定义和当次搭建参数补全。 |
| R13 | 清理本次新增或直接修改代码中的冗余 `Objects.requireNonNull`；仅在延迟 supplier、公开构造不变量或确有必要的过滤边界保留。 |
| R14 | 读取、检索、检查和构建已在 IntelliJ IDEA 打开的工程时优先使用 IDEA MCP；隔离 worktree 未在 IDEA 打开时才使用终端。 |
| R15 | Trinity 多方块主机必须提供 `main`、`cpu`、`crafting` 与自动搭建共四个可同时打开的独立子 UI；自动搭建不是结构窗口内的页签。四个子 UI 各自持有拖动位置、z-order、元素树、`Scene`/资源和释放周期，Esc 每次只关闭当前最上层子 UI。 |
| R16 | 所有含 LDLib2 sync/RPC 的 host 元素必须在 client/server 以确定顺序构造同构注册拓扑。动态窗口 membership 由服务端权威控制，open/close/reopen 必须通过始终挂载的 root coordinator 按相同顺序应用；禁止用预挂载隐藏窗口规避生命周期。 |
| R17 | 预览选择必须区分稳定的 `structureKey`、结构内部 `variantIndex` 与仅影响显示的 `visibleLayer`；XEI 的 `registeredRecipeId` 保持 controller 级稳定，动态选择使用独立 `projectionFingerprint` 校验。 |
| R18 | 只有规范化材料汇总槽可标记 XEI `INPUT`，controller/owner 规范槽标记 `OUTPUT`；`Scene`、候选检查器、谓词详情、hover 和层级控件均为 `NONE`，不得污染 recipe transfer。 |
| R19 | `structureKey`、`variantIndex`、tier/repeat 与候选选择产生的材料及数量会反映到普通 Processing 样板，但这些选择标识本身不得作为 metadata、custom pattern details 或 `BlockState` 写入样板。 |
| R20 | Lombok 只用于确实减少样板代码的场景，例如简单 getter/setter；不得为了使用 Lombok 改写清晰的领域模型。适合表达不可变数据载体和结构化值的 `record` 必须保留，不改成可变 POJO。 |
| R21 | JEI/EMI transfer 只把服务端按当前 catalog 权威重建的普通 input/output 填入 AE Processing 配置槽并切换模式；不得直接编码、写 `encodedPatternSlot`、消耗空白样板或搬运物料。只有用户点击 AE 自带编码按钮时才生成普通 Processing Pattern。 |
| R22 | 客户端 XEI 验收必须实际覆盖 JEI/EMI 渲染和按钮交互；服务端 GameTest 或 plain JUnit 通过不能替代客户端验收，也不得通过修改生产依赖绕过外部客户端启动问题。 |
| R23 | 普通多方块 transfer 必须把 AE 菜单残留的 pending/last source、key、fluid 和 display fallback 纳入同一原子事务：成功前清空并读回验证，失败与 mode/input/output 一起精确回滚。Data Ripper 特殊栈只可从明确的 Data Ripper recipe/holder 解析，编码前自动 key 解析还必须有明确的 Data Ripper pending source，不能污染普通 Processing Pattern。 |
| R24 | JEI 可选 `ae2jeiintegration` 必须用 mod-loaded 条件和独立类隔离直接类型引用，不使用反射；EMI 多方块 handler 只响应 `FILL_BUTTON`，并让 AE2 父类 catch-all handler 对 DataE typed recipe 正确 defer。 |

## Gradle 与 Git 约束

1. Gradle 只能使用系统环境变量提供的 `GRADLE_USER_HOME`。执行前必须读取并记录其值；未设置或解析后的路径不位于 `E:/` 盘时停止并报告，不能自行创建或重定向 `.gradle` 目录。
2. 不通过命令行 `-g`、项目属性、脚本或 IDE 设置覆盖系统环境变量。
3. 当前检测到的机器级 `GRADLE_USER_HOME` 为 `E:\.gradle`，但后续会话仍必须重新读取，不能依赖本文的历史值。
4. Data Energistics 与 Modular Data Lib 是独立 Git 事务；两个分支和 Draft PR 均已建立并获准继续推送，不需要重复请求授权。
5. MDLib 的后续修正只允许在 `F:/mc/Fish_Dan_/Modular-Data-lib-preview-api` 完成。原始 `F:/mc/Fish_Dan_/Modular-Data-lib` 只可只读参考，不能覆盖、暂存或提交其中的用户改动。
6. Data Energistics 必须保留未跟踪的 `logs/`、`wenli/`；MDLib 隔离 worktree 必须保留未跟踪的 `logs/`，任何仓库都不得使用会误纳这些目录的宽泛暂存命令。

## 当前实施进度

1. 本次文档同步的 Git 锚点是 `b5724470 清理 Objects.requireNonNull 滥用`，本地 `HEAD` 与远端 `origin/qy/ldlib2-ui-migration` 均位于该提交。P0-P7 的生产实现已按功能拆分提交，不能退回 P3 重做。
2. MDLib 的中立 projector/paired-candidate API 已在隔离 worktree 和 Draft PR #2 完成；`PatternCandidate` 绑定 exact preview `BlockState` 与 component-aware placement `ItemStack`，歧义 many-to-many Fail Fast。其 `spotlessCheck test runGameTestServer build` 与 GameTest 4/4 已通过。除非确认存在新的中立 API 缺口，不再扩大 MDLib 范围。
3. 已提交的多方块 UI 包括 Trinity Data Core、Pattern Core、四个真实业务 provider、双端服务端权威窗口 lifecycle、退款与自动搭建动作、共用 preview session/panel/factory 和隔离的 `Scene` 生命周期。host 按 `main -> cpu -> crafting -> auto_build` 注册；四窗均为独立可拖动子 UI，`auto_build` 不是结构页签。旧 Trinity Data Core 与旧自动搭建生产入口已移除。
4. 已提交的舱室 UI 包括 `ME_OUTPUT`、`ME_INPUT`、INPUT/OUTPUT 组合仓库和 `PATTERN_BUFFER`；均复用既有菜单槽、AE fake slot、容量与升级规则。P9 仍需做一次生产注册和残留旧入口总扫描。
5. 通用 XEI preview composition、JEI 页面和 EMI 页面已提交。JEI/EMI 共用 typed recipe view 与纯 LDLib2 UI factory，支持 `structureKey`、`variantIndex`、tier/repeat、candidate 和逻辑层/显示层控制，并隔离 recipe role。
6. `b5724470` 后当前工作区已实现 P8-A common/network typed transfer：bounded payload/codec、完整 `projectionFingerprint`、服务端按当前 catalog 权威重建、真实 `ConfigInventory` 容量/过滤/数量预检、双库存 batch 原子写入、逐槽读回、完整回滚和不完整回滚后的菜单失效。请求失败日志只记录有界 identity 和 selection 数量。成功只切换 `PROCESSING` 并填配置，不调用编码、不碰 `encodedPatternSlot`、不搬物料。
7. P8-A 同时以不可变 `PatternEncodingMultiblockTransferState` 快照 pending/last source、pending key input/output、pending fluid input/output 和 key display fallback。提交阶段清空并验证全部字段；任一步失败时连同 mode/input/output 精确回滚。XEI Data Ripper resolver 只接受明确的 `DataRipperReassemblerRecipe` 或其 `RecipeHolder`，编码前自动 key 解析还要求明确的 Data Ripper pending source，普通多方块配方不会继承旧 Data Ripper 状态。
8. 当前工作区已实现 P8-B JEI 与 P8-C EMI typed handler。两端都在点击瞬间读取唯一 `currentRecipeView()`，不信任缓存 ingredient。JEI 对 source/identity、真实 slot filter 和 `maxAmount` 异常做请求前预检；可选 `ae2jeiintegration` 通过 mod-loaded 条件和独立类直接接入，不使用反射。EMI 只处理 `FILL_BUTTON`，并在具体 handler 上修复继承的 `supportsRecipe` catch-all。JEI 与 EMI 定向 JUnit 均为 7/7。
9. P8-A 已通过 IDEA 定向 build、`compileJava compileTestJava`、codec JUnit 和完整 `runGameTestServer`，服务端 GameTest 为 356/356。P8-A/B/C 尚未按功能拆分提交；统一 Spotless、全量集成验证、最终 diff/编码/质量审计及 Draft PR 更新仍待主代理完成。
10. 客户端 GameTest 在启动阶段因 Oritech 的 Athena 运行时依赖缺失而阻塞。不得修改依赖规避，也不得把服务端/JUnit 通过等同于 JEI/EMI 客户端渲染、按钮和资源释放验收；外部运行环境修复后补跑。

## 术语固定

| 术语 | 含义 |
| --- | --- |
| 结构定义 | 一个可解析的多方块定义，例如 Trinity main。 |
| 子结构 | 同一控制器下可独立选择和配置的结构成员，例如 main、cpu、crafting。 |
| 等级 | 决定该子结构中可替换部件档位的业务参数。 |
| 重复层数 | 决定可重复结构段展开次数的业务参数。 |
| 显示层 | 只影响 3D 预览可见坐标的切片，不改变结构或材料。 |
| `structureKey` | controller 下具名子结构的稳定身份，例如 `main`、`cpu`、`crafting`。 |
| `variantIndex` | 同一具名子结构内部的稳定 shape/variant 选择，不等同于 repeat 或显示层。 |
| `visibleLayer` | `PreviewViewState` 中的显示切片；`ALL` 必须有明确表示，永不参与材料或样板。 |
| `registeredRecipeId` | XEI 注册时稳定的 controller 级 recipe id，不随页面内选择改变。 |
| `projectionFingerprint` | definition revision 与当前结构、variant、tier/repeat、候选选择共同形成的动态投影校验值。 |
| 预览快照 | 给定结构配置后生成的不可变相对坐标、谓词、候选、材料、边界和层信息。 |
| 多方块配方 | GT/ECO 风格的 XEI recipe wrapper；input 是结构材料，output 是控制器/owner 物品，不新增 AE 专属配方类型。 |
| XEI | JEI、EMI，以及未来可能接入的 REI 的统称。 |

## 上下文恢复步骤

1. 先完整阅读本文件、[GOAL.md](GOAL.md) 和 [07-AE-样板写入设计.md](07-AE-样板写入设计.md)。
2. 检查用户最新消息是否改变实施授权；最新约束优先于本文。
3. 读取三个参考仓库：`F:/mc/ldlib/LDLib2`、`F:/1.21.1/NeoECOAEExtension`、`F:/mc/ExtremelyFrozen/fork/GregTech-Modern`；优先通过 IDEA MCP 读取已打开工程。
4. MDLib 实现和 PR 只检查 `F:/mc/Fish_Dan_/Modular-Data-lib-preview-api`；原始 `F:/mc/Fish_Dan_/Modular-Data-lib` 仅在确有参考需要时只读访问。
5. 每次构建前重新读取系统 `GRADLE_USER_HOME`；未设置或解析路径不位于 `E:/` 盘时停止，不创建替代目录。
6. 核对上文两个 Draft PR、当前分支和工作区状态，从 `b5724470` 后的 P8-A/B/C 集成、拆分提交与 P9 发布验收继续；不得重复已完成的 P2A、P3、舱室和 XEI 页面工作。

## 已确认的动态窗口协议

1. `MenuTypeBuilder` 在服务端与客户端分别调用同一个菜单 factory；菜单完成 AE 槽创建后立即构造并挂载静态 host root。所有 sync value、RPC 和 message coordinator 必须以确定顺序注册，不能因物理侧、语言或本地配置改变注册拓扑。
2. 始终挂载的 root coordinator 传输 `{operation, key, sequence}`。客户端只提交 request；服务端验证 provider key、宿主有效性和当前 membership 后先 open/close 自己的动态树，再返回 ack；客户端仅按匹配 ack 应用同一操作。拒绝或乱序响应不得改变客户端树。
3. 关闭按钮和 Esc 都必须走 coordinator，不能先在客户端 `requestClose()` 移除按钮所属树。LDLib2 2.2.8 会在本地 listener 之后才发送元素级 server RPC，先移除会导致发包入口消失并留下服务端窗口。
4. LDLib2 sync/RPC ID 按注册顺序单调分配，移除后不复用。双方必须按相同 sequence 执行 open/close/reopen，provider 每次返回新的树；不同步的相机、拖动位置、z-order、hover 和 `visibleLayer` 只保留在客户端。
5. coordinator 是静态同步协议节点，不是隐藏浮窗。ECO 的预挂载 `display: none` 模式只可参考 factory/binding，禁止用于本项目四窗生命周期。

## 已确认的配方语义

用户已确认不设计“AE 专属多方块配方”或“建造配置样板”。实现借鉴 GT/ECO 的 XEI 多方块页面展示方式，并由 Data Energistics 明确定义普通 recipe view 与 typed transfer：当前展开结构的规范材料清单是输入，控制器/owner 物品是输出；AE 样板功能只是对该配方执行现有 transfer 和 Processing 编码。GT/ECO 均不能作为完整 AE writer 的既成实现。

样板必须保持通用：它不能成为结构选择或方块状态的序列化载体。`structureKey`、`variantIndex`、tier/repeat 和候选选择只通过最终普通 stack/amount 影响样板内容，其身份值不进入样板。预览可用精确 `BlockState` 渲染，材料映射只取可放置物品；执行自动搭建时才由独立且经服务端校验的 build request 携带结构与搭建参数，并依据权威定义生成实际放置状态。

AE 菜单还可能保留上一次 recipe transfer 的 workstation source、Data Ripper key/fluid 和 display fallback。这些不是普通多方块配方的一部分，必须在填入普通 Processing 配置的同一事务中清空；清理失败或后续提交失败必须恢复完整旧状态。Data Ripper resolver 只能在传入对象明确是 Data Ripper recipe 或 holder 时返回结果，不能从普通 XEI 上下文、显示文本或历史 source 猜测。
