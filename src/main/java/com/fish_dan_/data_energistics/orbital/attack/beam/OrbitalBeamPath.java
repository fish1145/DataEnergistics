package com.fish_dan_.data_energistics.orbital.attack.beam;

/** Persisted traversal format; old cursors retain their original vertical-column ordering. */
public enum OrbitalBeamPath {
    VERTICAL_COLUMNS,
    AIMED_RAYS
}
