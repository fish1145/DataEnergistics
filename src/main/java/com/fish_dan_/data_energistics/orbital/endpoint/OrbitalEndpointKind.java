package com.fish_dan_.data_energistics.orbital.endpoint;

/**
 * Physical block kinds that can provide an uplink between an AE grid and an orbital weapon.
 */
public enum OrbitalEndpointKind {
    CONTROL_CONSOLE,
    UPLINK_BEACON
}
